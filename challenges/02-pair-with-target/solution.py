"""02 — pair-with-target. First pass in plain text (see ../../CONSTRAINTS.md)."""


def two_sum(nums, target):
    """Return (i, j) with i < j and nums[i] + nums[j] == target, or None.

    If several pairs work, return the one with the smallest second index.

    >>> two_sum([2, 7, 11, 15], 9)
    (0, 1)
    """
    raise NotImplementedError


def all_pairs(nums, target):
    """Follow-up. Return all unique value-pairs (small, large) summing to target, as a sorted
    list of tuples. Duplicates in the input must not duplicate pairs. No solution -> []."""
    raise NotImplementedError

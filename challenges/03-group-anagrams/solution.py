"""03 — group-anagrams. First pass in plain text (see ../../CONSTRAINTS.md)."""


def group_anagrams(words):
    """Group anagrams together. Groups are ordered by first appearance; words keep input
    order within a group. Case- and space-sensitive.

    >>> group_anagrams(["eat", "tea", "tan", "ate", "nat", "bat"])
    [['eat', 'tea', 'ate'], ['tan', 'nat'], ['bat']]
    """
    raise NotImplementedError


def group_anagrams_normalized(words):
    """Follow-up. Group case-insensitively and ignoring spaces; return the original words."""
    raise NotImplementedError

"""05 — merge-sorted. First pass in plain text (see ../../CONSTRAINTS.md)."""


def merge(a, b):
    """Merge two ascending-sorted lists into one. O(n + m), keeps duplicates, new list.

    >>> merge([1, 3, 5], [2, 4, 6])
    [1, 2, 3, 4, 5, 6]
    """
    raise NotImplementedError


def merge_k(lists):
    """Follow-up. Merge k sorted lists into one sorted list. Handles empty input/empties."""
    raise NotImplementedError
